import { EvidenceAssessmentPipe } from './evidence-assessment.pipe';

describe('EvidenceAssessmentPipe', () => {
  it('create an instance', () => {
    const pipe = new EvidenceAssessmentPipe();
    expect(pipe).toBeTruthy();
  });
});
