import { AssessmentTargetPipe } from './assessment-target.pipe';

describe('AssessmentTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new AssessmentTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
